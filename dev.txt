# init gh-pages
git checkout --orphan gh-pages
git rm -rf .
echo "helm-charts" > index.html
git add index.html
git commit -a -m "add index.html"
git push origin -u gh-pages

https://edgelevel.github.io/helm-charts/

---

# publish
git clone -b gh-pages git@github.com:edgelevel/helm-charts.git tmp-gh-pages
cd tmp-gh-pages/
helm lint ../argocd
helm package ../argocd
helm repo index .
git add .
git commit -m "release argocd"
git push origin gh-pages
cd ..
rm -fr tmp-gh-pages/
